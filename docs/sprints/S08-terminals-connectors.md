# S8 · Клеммники и разъёмы

- **Веха:** B — Данные и отчёты
- **Статус:** 🚧 В работе (таблица клемм готова) · ветка `phase-2-symbols`
- **Зависит от:** S5

## Цель
Клеммники (XT) и разъёмы с генерацией таблиц.

## Scope
- [x] **Таблица клемм** (табличная генерация): `core/reports.ts` `computeTerminals` — для каждого символа-клеммы (`kind:"terminal"`) что подключено к каждому выводу (номер цепи + соседние выводы устройств); `terminalsToCsv`. UI: «Схема» → «Клеммы» (таблица + CSV + печать). +тесты (3).
- [x] **Графический клеммник (схема рейки)**: `computeTerminalStrips` (группировка клемм в рейки по префиксу сиглы, числовая сортировка — связность вычисляется, принцип 2) +тест; рендер `apps/desktop/src/terminal-strip.ts` (вертикальный стек блоков, подключения слева/справа). UI: «Схема» → «Клеммник» (диалог + печать SVG).
- [ ] Физическая раскладка клеммника (1:10, многоуровневая) — требует layout-движок, ближе к S10.
- [ ] `Connector` + пины разъёма из каталога — позже (после формата `*.part.json`/S6+).
- [ ] `Wire2Terminal` (явная модель клеммы с сортировкой/нумерацией выводов) — позже.

## DoD
- [x] клеммник с таблицей соединений (по выводам)
- [x] графическая схема клеммника (рейка с подключениями + печать)
- [ ] физическая раскладка 1:10 + разъёмы — позже (S10/S12)
