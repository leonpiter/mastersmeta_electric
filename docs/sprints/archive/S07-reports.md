# S7 · Генерация отчётов

- **Веха:** B — Данные и отчёты
- **Статус:** ✅ Готов · Дата окончания: 2026-05-26
- **Зависит от:** S5, S6

## Цель
Документы из схемы: перечень элементов и таблица соединений.

## Scope
- [x] Перечень элементов (ГОСТ 2.701): `core/reports.ts` — `computeBom` (строка на устройство, наименование = УГО + изделие каталога, артикул в примечании, натуральная сортировка сигл) + `bomToCsv`. «Живой» — вычисляется из проекта.
- [x] UI: вкладка «Схема» → «Перечень»/«Соединения» — таблицы, экспорт CSV (UTF-8 BOM для Excel), печать/PDF (окно A4).
- [x] Таблица соединений (из `computeNets`): по цепям — соединяемые выводы «сигла:вывод», номер цепи, провод (тип/сечение), лист. `computeConnections` + `connectionsToCsv`.
- [ ] Точная ГОСТ-вёрстка колонок (по скрину «Zestawienie»), группировка одинаковых позиций (кол-во > 1, диапазон сигл), pin-to-pin маршрутизация, Excel round-trip правки кодов — позже.

## DoD
- [x] из схемы получаем перечень элементов **и таблицу соединений**; экспорт CSV + печать/PDF
- [x] тесты ядра (`reports.test.ts` +7: устройство=строка, артикул, сортировка, CSV, цепь→выводы, висящие, CSV соединений); Playwright-приёмка
- [ ] Excel round-trip правки кодов — позже

## Открытые вопросы
- Точная вёрстка колонок перечня по ГОСТ — по образцу/скриншоту «Zestawienie» из установленной версии (для финальной вёрстки и группировки).

## Итоговый отчёт
**Дата:** 2026-05-26 · **Итог:** 2/3 scope + перечень и соединения (закрывает критический путь MVP S1→S7)

### Выполнено
`core/reports.ts`: `computeBom` (строка на устройство, натуральная сортировка) + `computeConnections` (по цепям: соединяемые выводы «сигла:вывод», номер цепи, провод, лист) + CSV-экспорт. UI: вкладка «Схема» → «Перечень»/«Соединения» — таблицы, экспорт CSV (UTF-8 BOM для Excel), печать/PDF (A4). Рендер отчётов — общие `showReport`/`buildTable`/`printReport`/`downloadCsv`.
### Не выполнено (осознанно отложено)
Точная ГОСТ-вёрстка колонок (ждёт скрин «Zestawienie»), группировка одинаковых позиций, pin-to-pin маршрутизация, Excel round-trip.
### Уроки
Таблица соединений по цепям (а не pin-to-pin) — честное представление того, что вычислимо из модели (Net не хранит маршрутизацию провод↔пара выводов). Завершение S7 закрыло сквозной рабочий цикл редактора.
