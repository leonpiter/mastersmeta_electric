# S14 · Core API (низкоуровневые операции)

- **Веха:** F — LLM-генерация
- **Статус:** 📝 Запланирован
- **Зависит от:** S5–S7

## Цель
Типизированный API ядра, пригодный для программной сборки схем (фундамент для LLM).

## Scope
- [ ] Методы: `addComponent`, `connect`, `numberWires`, `generateBOM`, `renderSheet`.
- [ ] Headless-рендер SVG/PNG для само-верификации агента (рендер → картинка → проверка).

## DoD
- [ ] схема собирается кодом и рендерится без GUI; покрыто тестами
