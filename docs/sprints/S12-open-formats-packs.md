# S12 · Открытые форматы и библиотечные паки

- **Веха:** E — Сообщество / OSS
- **Статус:** 🚧 В работе · ветка `phase-2-symbols`
- **Зависит от:** S2 / S6

## Scope
- [ ] JSON Schema для `*.symbol.json` / `*.part.json` + валидация в CI.
- [x] Библиотечные паки (manifest `formatVersion`/`name`/`license` + контент: категории+УГО+блоки); экспорт/импорт (= установка) через меню «Файл». Ядро: `core/packs.ts` (`buildPack`/`validatePack`/`serializePack`/`parsePack`, лоялен к битым элементам) +тесты; импорт обратно совместим со старым форматом (массив `SymbolDef`).
- [ ] Решение по лицензиям: код **MIT** (решено в S20); контент (CC-BY) — поле `license` в паке есть, финальное решение к публикации реестра.
- [ ] Community-реестр / публикация паков (→ S13).

## DoD
- [x] пак собирается, экспортируется в `.mpack` и импортируется обратно (категории+символы+блоки), мёрж в localStorage+реестр+библиотеку; round-trip проверен Playwright.
- [ ] пак валидируется JSON Schema в CI и ставится из реестра.

## Состояние
**Сделано:** локальный пак (`core/packs.ts`) — экспорт/импорт категорий+УГО+блоков одним файлом
`.mpack` (JSON), манифест с версией/лицензией, устойчивый импорт + обратная совместимость.
**Дальше:** JSON Schema (`*.symbol.json`/`*.part.json`) + ajv-валидация в CI; реестр/публикация (S13).
