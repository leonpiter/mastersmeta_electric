# S27 · Полноценный редактор УГО и сложные компоненты

- **Веха:** C — Создание контента (расширяет S9)
- **Статус:** 🚧 В работе · ветка `phase-2-symbols`
- **Зависит от:** S9 (редактор), S5 (master/slave), S26 (вкладки страниц)

## Цель
УГО охватывают весь спектр: мелкие контакты/кнопки → крупные приборы (контроллеры/ПЛК,
реле времени, аппараты) → готовые блоки управления со своими клеммниками. Доменная модель
`SymbolDef` уже поддерживает символы любого размера/числа выводов — узкое место в редакторе.

## Scope (фазами)
- [x] **Ф1. Редактор с масштабированием** — pan/zoom (по образцу canvas.ts: колесо-зум от курсора, средняя/Space-панорама), адаптивная сетка-pattern, «Вписать» (по содержимому), индикатор зума, инструмент «Текст», большое поле 540×460, многовыводность (авто-инкремент имён), **инструмент «Выбрать» — выбор/перетаскивание/удаление (Del) элемента**. Без изменения ядра.
- [ ] **Ф2. Зеркало контактов: таблица M/NO/NC + переход** — авто-выноска под катушкой (`SymbolDef.contacts`), боксовая таблица, двойной клик по контакту → переход на его лист (S26 `activatePage`).
- [ ] **Ф3. Множественные подписи** — QF1 / 10А / С: `SymbolInstance.attributes/labelFields/labelOffset`, `instanceLabels`, рендер + диалог свойств.
- [ ] **Ф4. Составные блоки (макрос-группа)** — `core/blocks.ts` (`captureBlock`/`InsertBlockCommand`), мультивыделение, «Сохранить как блок», секция «Блоки» в панели; раскрытие при вставке с авто-перенумерацией.
- [ ] **Ф5. Типизированные категории оборудования** (решение пользователя) — строгие категории-классы (`EquipmentCategory`: код+kind+схема характеристик); символ живёт в своей категории (перенос запрещён); базовый комплект `GOST_CATEGORIES` + пользовательские категории; характеристики категории питают подписи/свойства (Ф3 data-driven); экспорт/шеринг своих категорий+УГО (пак, S12).
  - [x] Ядро: `categories.ts` (`AttrDef`/`EquipmentCategory`/`GOST_CATEGORIES`/`CategoryRegistry`), переклассификация встроенных УГО в типизированные категории, +тесты.
  - [ ] Редактор: «Категория» — выпадающий список (задаёт код+допустимые kind), без свободного ввода/переноса; своя категория.
  - [ ] Библиотека: группировка по категориям, «мои» — подгруппа внутри категории; экспорт/импорт категорий+символов.
  - [ ] Свойства/подписи — из `category.attributes` (объединяет Ф3 и каталог S6).

## Технический дизайн
- Крупные символы = апгрейд редактора: `panX/panY/zoom`, `scalePx=PX_PER_MM*zoom`, content `<g>` transform, сетка-`<pattern>`, `fit()` через `symbolBounds`.
- Блоки — «макрос»: выделенные инстансы → шаблон относительных размещений; вставка строит обычные инстансы (одна `MacroCommand`/`InsertBlockCommand`), последовательная авто-перенумерация. Плоская модель и master/slave/отчёты работают без изменений.
- Зеркало контактов: `SymbolDef.contacts?: {pins; column:"M"|"NO"|"NC"}[]`; `coilContactRows(device, coilSym)` сливает комплект с размещёнными (адрес «лист.зона»).

## DoD
- [ ] Ф1: рисуется крупный многовыводной символ (zoom/pan/вписать/текст), сохраняется, ставится, выводы в связности.
- [ ] Ф2: таблица под катушкой с адресами; двойной клик → переход на лист контакта.
- [ ] Ф3: у элемента несколько подписей (сигла/номинал/характеристика).
- [ ] Ф4: блок из нескольких УГО (+клеммы) собирается и вставляется целиком; отчёты подхватывают.
- [ ] `pnpm verify` зелёный; визуальная приёмка через Playwright по каждой фазе.

## Итоговый отчёт
<!-- заполняется /sprint-finalize -->
