# S27 · Полноценный редактор УГО и сложные компоненты

- **Веха:** C — Создание контента (расширяет S9)
- **Статус:** 🚧 В работе · ветка `phase-2-symbols`
- **Зависит от:** S9 (редактор), S5 (master/slave), S26 (вкладки страниц)

## Цель
УГО охватывают весь спектр: мелкие контакты/кнопки → крупные приборы (контроллеры/ПЛК,
реле времени, аппараты) → готовые блоки управления со своими клеммниками. Доменная модель
`SymbolDef` уже поддерживает символы любого размера/числа выводов — узкое место в редакторе.

## Scope (фазами)
- [x] **Ф1. Редактор с масштабированием** — pan/zoom (по образцу canvas.ts: колесо-зум от курсора, средняя/Space-панорама), адаптивная сетка-pattern, «Вписать» (по содержимому), индикатор зума, инструмент «Текст», большое поле 540×460, многовыводность (авто-инкремент имён), **инструмент «Выбрать» — выбор/перетаскивание/удаление (Del) элемента**. Без изменения ядра.
- [x] **Ф2. Зеркало контактов: таблица M/НО/НЗ + переход** — боксовая таблица под катушкой (`coilContactRows` в ядре + рендер в `canvas.ts`): колонки M/НО/НЗ, выводы и адрес «лист.зона»; двойной клик по адресу → переход на лист контакта (`onNavigateToContact` → `activatePage` + `focusInstance`). _Полный комплект контактов из `SymbolDef.contacts` (показ незанятых) — отложено._
- [x] **Ф3. Множественные подписи** — QF1 / 10А / С: `SymbolInstance.attributes/labelFields` + чистая `instanceLabels`, стопка подписей в `canvas.ts`, секция «Характеристики» в диалоге свойств (поля из `category.attributes`, флаг «на схеме»). `EditInstanceCommand` расширена (+тесты). _Перетаскивание отдельных подписей (`labelOffset`) и значения из каталога — отложено._
- [ ] **Ф4. Составные блоки (макрос-группа)** — `core/blocks.ts` (`captureBlock`/`InsertBlockCommand`), мультивыделение, «Сохранить как блок», секция «Блоки» в панели; раскрытие при вставке с авто-перенумерацией.
- [ ] **Ф5. Типизированные категории оборудования** (решение пользователя) — строгие категории-классы (`EquipmentCategory`: код+kind+схема характеристик); символ живёт в своей категории (перенос запрещён); базовый комплект `GOST_CATEGORIES` + пользовательские категории; характеристики категории питают подписи/свойства (Ф3 data-driven); экспорт/шеринг своих категорий+УГО (пак, S12).
  - [x] Ядро: `categories.ts` (`AttrDef`/`EquipmentCategory`/`GOST_CATEGORIES`/`CategoryRegistry`/`validateCategory`), переклассификация встроенных УГО в типизированные категории, +тесты.
  - [x] Редактор: «Категория» — выпадающий список из реестра (задаёт код read-only + ограничивает kind); пункт «➕ Новая категория…» → диалог; `user-categories.ts` (localStorage). Перенос между категориями невозможен.
  - [x] Библиотека: группировка по категориям + подпапка «Мои символы» внутри категории; контекст-меню «Копировать…»/«Переименовать…» (быстрое переименование своих УГО). _Экспорт/импорт пака (категории+символы) — отложено к S12._
  - [x] Свойства/подписи — из `category.attributes` (объединяет Ф3 и каталог S6): секция «Характеристики» в диалоге свойств.

## Технический дизайн
- Крупные символы = апгрейд редактора: `panX/panY/zoom`, `scalePx=PX_PER_MM*zoom`, content `<g>` transform, сетка-`<pattern>`, `fit()` через `symbolBounds`.
- Блоки — «макрос»: выделенные инстансы → шаблон относительных размещений; вставка строит обычные инстансы (одна `MacroCommand`/`InsertBlockCommand`), последовательная авто-перенумерация. Плоская модель и master/slave/отчёты работают без изменений.
- Зеркало контактов: `SymbolDef.contacts?: {pins; column:"M"|"NO"|"NC"}[]`; `coilContactRows(device, coilSym)` сливает комплект с размещёнными (адрес «лист.зона»).

## DoD
- [x] Ф1: рисуется крупный многовыводной символ (zoom/pan/вписать/текст), сохраняется, ставится, выводы в связности.
- [x] Ф2: боксовая таблица M/НО/НЗ под катушкой с адресами; двойной клик по адресу → переход на лист контакта.
- [x] Ф3/Ф5: у элемента несколько подписей из характеристик категории (сигла/номинал/характеристика) — QF1 / 10А / C.
- [ ] Ф4: блок из нескольких УГО (+клеммы) собирается и вставляется целиком; отчёты подхватывают.
- [ ] `pnpm verify` зелёный; визуальная приёмка через Playwright по каждой фазе.

## Состояние и следующий шаг (для продолжения)
**Сделано (коммиты на ветке `phase-2-symbols`):**
- Ф1 — редактор pan/zoom + текст + выбор/перемещение/Del (`0b9a136`).
- Ф5-ядро — `categories.ts` + переклассификация УГО + тесты (`2a963b8`).
- Ф5-редактор — `#se-cat` выпадающий список из `CategoryRegistry` (код read-only + фильтр kind), диалог «Новая категория», `user-categories.ts` (localStorage), `validateCategory` + тесты (`3aa4478`).
- Ф3 — подписи из характеристик категории: `SymbolInstance.attributes/labelFields` + `instanceLabels`, стопка подписей в `canvas.ts`, секция «Характеристики» в `#props`, расширена `EditInstanceCommand` + тесты.
- Библиотека — подпапка «Мои символы» внутри категории (`isUser` предикат + `subFolder`), контекст-меню «Копировать…»/«Переименовать…», диалог `#rename-dialog`.
- Ф2 — боксовое зеркало контактов M/НО/НЗ под катушкой (`coilContactRows` + `renderContactMirror`), переход по dblclick на адрес (`onNavigateToContact` → `activatePage` + `focusInstance`) + тест.

**Дальше по порядку (выбранное направление — строгие категории):**
1. **Ф4** — составные блоки (`core/blocks.ts` + `InsertBlockCommand` + мультивыделение).
2. **S12** — экспорт/импорт пака (категории+символы), встраивание кастомных УГО в `.esch`; полный комплект контактов из `SymbolDef.contacts`.

**Помнить:** dblclick по канвасу в Playwright гасится pointer-capture (диспатчить событие); кликать по телу УГО; `verify` без пайпа; перед коммитом `SEE_SKIP_TURN_GATE=1 pnpm verify` зелёный.

## Итоговый отчёт
<!-- заполняется /sprint-finalize -->
